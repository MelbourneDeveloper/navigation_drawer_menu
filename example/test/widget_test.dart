import 'package:flutter_test/flutter_test.dart';
import 'package:navigation_drawer_menu_example/main.dart';

void main() {
  testWidgets('Empty', (tester) async {
    // Build our app and trigger a frame.
    await tester.pumpWidget(const MyApp());

    //Tests are coming
  });
}
